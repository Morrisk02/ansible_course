- hosts: all
  become: true
  tasks:

    - name: copy files to remote clients
      tags: copy
      copy:
        src: default_site.html
        dest: /var/www/html/index.html
        owner: root
        group: root
        mode: 0644
